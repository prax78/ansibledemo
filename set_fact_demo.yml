---
- hosts: all
  gather_facts: false
  vars:
   my_var: "test"
  tasks:
    - name: Get IP Address
      win_shell:  Get-NetIPAddress |?{$_.addressfamily -eq 'IPv4'}| select IPAddress
      register: ip_output
    
    - debug:   
       var: ip_output.stdout
      

